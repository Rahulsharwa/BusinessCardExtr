version: "3.8"

services:
  api:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./local_images:/data/images # Mount for local-folder mode
    restart: unless-stopped
